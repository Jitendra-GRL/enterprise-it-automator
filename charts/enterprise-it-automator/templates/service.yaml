apiVersion: v1
kind: Service
metadata:
  name: {{ include "eit.fullname" . }}
  labels:
    {{- include "eit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "eit.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
